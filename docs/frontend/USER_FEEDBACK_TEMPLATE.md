# User Feedback Template (Migration Rollout)

Use this template to collect post-rollout feedback for migrated React flows.

## Metadata

- Date:
- Reporter:
- Environment: (`staging` / `production`)
- Route:
- Browser/Device:

## Feedback Type

- Category: (`bug` / `ux` / `performance` / `feature request`)
- Severity: (`critical` / `high` / `medium` / `low`)

## Description

- Expected behavior:
- Actual behavior:
- Repro steps:

## Evidence

- Screenshot/video:
- Console errors:
- `X-Request-Id` / trace ID:

## Triage

- Owner:
- Linked issue:
- Status: (`new` / `triaged` / `in-progress` / `resolved`)
