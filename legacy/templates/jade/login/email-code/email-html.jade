h3 Sign in to #{projectName}
p Your one-time sign-in code is:
p(style='font-size: 28px; font-weight: bold; letter-spacing: 6px;') #{code}
p This code expires in #{expiresMinutes} minutes and can be used only once.
p
  a(href='#{signInLink}') Open the secure sign-in confirmation
p If you did not request this email, you can safely ignore it.
