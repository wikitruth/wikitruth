[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wikitruth-agent"
version = "0.1.0"
description = "Dependency-free client for Wikitruth's governed agent API"
requires-python = ">=3.9"
